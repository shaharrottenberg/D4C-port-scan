# D4C-port-scan
D4C port scan is as the name applies, a port scan code done in python.
it is very simple to run just download it and go (pls make sure you have the imported libraries in the top of the code, without them it wont run).
the code uses the very small and limited wordlists forged by myself for the sake of the project only.
a very simple code.
